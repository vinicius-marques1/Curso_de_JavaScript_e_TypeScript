services:
  mariadb:
    image: mariadb:latest
    container_name: bdmariadb
    restart: always

    ports:
      - "3306:3306"

    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}

    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data: